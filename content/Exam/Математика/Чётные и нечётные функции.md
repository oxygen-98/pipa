---
tags:
  - математика
aliases:
---
### Чётные и нечётные функции

#### Определение

Функция $f(x)$ называется чётной, если для любого $x$ из области определения выполняется условие:

$$ f(-x) = f(x).

$$

Функция $f(x)$ называется нечётной, если для любого $x$ из области определения выполняется условие:

$$ f(-x) = -f(x).

$$

#### Свойства

1. **Сумма и разность чётных/нечётных функций**:  
   - Сумма двух чётных функций — чётная функция.  
   - Сумма двух нечётных функций — нечётная функция.  
   - Сумма чётной и нечётной функций не обладает ни чётностью, ни нечётностью.  

2. **Произведение чётных/нечётных функций**:  
   - Произведение двух чётных функций — чётная функция.  
   - Произведение двух нечётных функций — чётная функция.  
   - Произведение чётной и нечётной функций — нечётная функция.

3. **Частное чётных/нечётных функций**:  
   - Частное двух чётных функций — чётная функция.  
   - Частное двух нечётных функций — чётная функция.  
   - Частное чётной и нечётной функций — нечётная функция.

4. **[[Интеграл]] чётной/нечётной функции**:  
   - [[Интеграл]] чётной функции на симметричном интервале $[−a, a]$ равен удвоенному интегралу на интервале $[0, a]$.  
   - [[Интеграл]] нечётной функции на симметричном интервале $[−a, a]$ равен нулю.

#### Примеры

##### Пример 1: Четная функция

Функция $f(x) = x^2$ является четной, так как:

$$ f(-x) = (-x)^2 = x^2 = f(x).

$$

##### Пример 2: Нечетная функция

Функция $f(x) = x^3$ является нечётной, так как:

$$ f(-x) = (-x)^3 = -x^3 = -f(x).

$$

